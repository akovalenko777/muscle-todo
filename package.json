{
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run start:dev -w apps/api\" \"npm run dev --host -w apps/web\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
